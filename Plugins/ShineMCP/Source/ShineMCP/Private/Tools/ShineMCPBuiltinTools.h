#pragma once

#include "CoreMinimal.h"

/**
 * ShineMCP 内置工具集：关卡 / 场景 / 捕获 / ComfyUI / 资产。
 */
class FShineMCPBuiltinTools
{
public:
    static void RegisterAll();
    static void UnregisterAll();
};
