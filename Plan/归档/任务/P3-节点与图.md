# P3 — 节点与图

> **施工图**（每个 S 的做法与判据）。勾选与状态在 `../PROGRESS.md`；实测证据（含每个 S 的 ✅ 记录）在 `../证据.md`。

> 一次只做一个 S：做完 configure → build → 运行，逐条对验收，再回 `../PROGRESS.md` 勾选。
> ⚠️ 早期步骤里写的「跑任务说明验收 N 条 / 按任务说明的日志样板」指的是**已于 2026-09-17 删除的 `说明/P3.md`**；本线已完工，逐条判据不再保留（结论见 `../证据.md` 与 `../PROGRESS.md`）。

## P3.0 — WS 事件与错误采集层（忙碌判定 + 错误全字段）  ✅ 10/10


- **S1 补齐 `PromptEvent` 字段** — 改 `src/comfy/ComfyTypes.h`：只**加**字段（`exceptionType` / `exceptionMessage` / `traceback` / `executedNodes` / `currentNodeType` / `timestampMillis`），不动既有字段与语义。判据：`cmake --build build -j` 通过，无调用点报错。
- **S2 WS 报文解析补全** — 改 `src/comfy/ComfySocket.h/.cpp`：`OnMessage(std::string_view)`（libhv 回调内做一次 `std::string` 转换）；解析 `{"type","data"}`；`execution_error` 取全字段；`progress_state` 更新进度；每个事件打一行 `[info] ws event ...`。判据：连上 ComfyUI 后日志能看到 event 行。
- **S3 完成语义 + 帧静默计时** — 同文件：`executing` 且 `data.node == null` 视为完成；新增帧时间戳与 `SinceLastFrame()`；每 15s 发 ping；**30s 无任何帧**才判定断开 → 转 `Connecting` 重连。判据：杀进程后 30s 内转 `Disconnected` 并写明原因。
- **S4 `BusyState` 与健康摘要** — 改 `src/comfy/ComfySession.h/.cpp`：按"判定规则"表实现 `Busy()` / `BusyDetail()` / `HealthSummary()` / `LastErrorDetail()` / `SinceLastEvent()` / `RequestInterruptCurrent()`；`BusyDetail` 指向内部缓存（帧内有效）。判据：5 种状态都能人工造出来并显示正确文案。
- **S5 队列行状态机** — 改 `src/comfy/ComfyQueueModel.h/.cpp`：事件按 `promptId` 落到对应任务行；`Interrupted` 与 `Failed` 分开；连续提交 3 个任务互不污染。判据：第 2 个失败时第 3 个仍正常执行，三行状态互不串。
- **S6 错误来源②：`/history` 覆盖式补齐** — `ComfySession`：WS 掉线漏收时，拉 `/history/{prompt_id}` 的 `status.messages`（`[事件名,数据]` 数组）补出完整错误，**覆盖式更新**不产生重复错误。判据：断线 10s 内重连仍能补出错误。
- **S7 错误来源③：`/prompt` 400 解析** — 解析 `{"error":{...}}` + `node_errors`，逐条转中文并标记到具体节点（供 P3.5 使用）。判据：模型名不存在时拿到节点级中文错误。
- **S8 日志规范落地** — 按任务说明里的日志样板实现：忙碌探活失败只打 `[warn] service busy, probe timeout ignored: ...` 且**不改连接状态**；错误打多行块（异常类型 / 消息 / 回溯前 3 行 / 中文 hint）；`queue:` 行仅在数值变化时每 5s 一条。
- **S9 UI 接线** — 改 `src/app/App.cpp`：状态栏显示 `HealthSummary()`；底栏「队列」显示"失败：<中文原因>"（hover 展开回溯）；侧栏 Comfy 面板显示忙碌详情；全局**不出现"卡死/断开"误判文案**。
- **S10 验收自测（6 条全跑）** — 逐条跑任务说明的验收 1–6，把结果贴进 `../PROGRESS.md` 证据栏。**未全过不进入 P3.1**。



---

## P3.1 — 节点定义 JSON（v2.0 优先 / v1.0 兼容）→ `NodeTypeDef` 结构化解析  ✅ 9/9


- **S1 骨架** — 新增 `src/comfy/ComfyNodeDef.h/.cpp`（数据模型 + `ParseNodeDefs` / `ParseNodeDef` 声明，实现先留空）；`CMakeLists.txt` 的 `add_executable` 登记。判据：编译通过。
- **S2 数据模型对齐 v2.0 规范** — 按 `Doc/RULES-COMFY.md` §12.6 A 定义 `NodeDefFormat{V1,V2}` / `WidgetKind{None,Int,Float,Bool,Text,Combo}` / `InputDef` / `OutputDef` / `NodeTypeDef`：输入公共字段（`isOptional`/`forceInput`/`hidden`/`advanced`/`lazy`/`rawLink`/`defaultInput`/`tooltip`）+ 类型专属（INT·FLOAT 的 `min`/`max`/`step`/`display`/`round`/`control_after_generate`；BOOLEAN 的 `labelOn`/`labelOff`；STRING 的 `multiline`/`dynamicPrompts`/`placeholder`；COMBO 的 `options`/`imageFolder`/`imageUpload`/`videoUpload`/`allowBatch`/`remoteRoute`）+ 三种默认值形态（各带 `has*` 标志）；`OutputDef` 带 `index`/`isList`/`tooltip`；`NodeTypeDef` 带 `description`/`pythonModule`/`outputNode`/`deprecated`/`experimental`/`format`。判据：字段与规范逐条对得上。
- **S3 解析 V2 形状：`inputs` 映射** — `inputs` 是**对象**（键 = 输入名，值 `{type,name,…}`）；`WidgetKind` 由 `type` 定（INT/FLOAT/BOOLEAN/STRING/COMBO → 控件，其余 → `None` 连线输入）；`isOptional` 取 `isOptional`；默认值按 JSON 类型落到对应字段；**保持 JSON 出现顺序**（v2 无 `input_order`）。判据：手写 V2 JSON 能解析出正确控件类型、范围与默认值。
- **S4 解析 V2 顶层与 `outputs`** — `name`/`display_name`/`description`/`category`/`output_node`/`python_module`/`deprecated`/`experimental`；`outputs[]` 取 `index`/`name`/`type`/`is_list`/`tooltip` 并**按 `index` 排序**。判据：V2 样例的 `LoadImage` 输出含 `IMAGE`/`MASK` 且 `isList` 正确。
- **S5 解析 V1 形状（只读兼容）** — `input.required|optional|hidden` 三个映射；单个输入是**定长二元数组** `[类型标识, 选项对象]`：字符串 → 类型名、**枚举数组 → COMBO 选项**；`isOptional` 按所属分组推断（`optional`/`hidden` → true）；输出侧用 `output`/`output_is_list`/`output_name` 拼 `OutputDef`（`index` 取数组下标）；`format = V1`。判据：`object_info` 现有响应（KSampler / CheckpointLoaderSimple / LoadImage）解析正确。
- **S6 自动识别形状 + 实测确认 V2 来源** — `ParseNodeDefs` 自动判定（**V2 判据：顶层有 `inputs` 对象且 `outputs[]` 项含 `index`/`is_list`**），不依赖外部开关；连本机 ComfyUI 拉 `GET /object_info` 与 `GET /object_info/{class}` 的**原始响应**，把"本机给的是哪种形状 + 关键片段"写进本文件「实测记录」（§12.6 D 硬性要求）；**禁止调用未证实的端点**。判据：日志能分别报出 `format=v1/v2` 的节点数。
- **S7 接进既有解析路径** — `src/comfy/ComfyTypes.h` 的 `ObjectInfoResult` 加 `std::vector<NodeTypeDef> nodes;` 与 `NodeDefFormat format;`；`src/comfy/ComfyClient.cpp:252-270` `ParseObjectInfoJson` 调 `ParseNodeDefs`，**保留** `rawJson` / `nodeClassCount` 既有语义；日志 `object_info: N classes parsed (v2 x / v1 y), M inputs`。判据：`ObjectInfoNodeCount()` 与改动前一致。
- **S8 `ComfySession` 缓存与查询** — `ObjectInfoNodes()` / `FindNodeDef(className)`（`[[nodiscard]]`，找不到返回 `nullptr`）；解析耗时 < 200ms。判据：日志数字合理。
- **S9 容错 + 验收** — 残缺/非法 JSON → 返回空列表 + `log::Warn`，**不崩溃**；未知类型不丢弃节点、只标记；跑「验收」全部条目并把结果贴 `../PROGRESS.md`。

---

## P3.2 — `ShineComfyNode` 通用可编辑节点  ✅ 7/7


- **S1 新建 `ComfyNode.h/.cpp` 骨架** — `class ShineComfyNode final : public VisNodeSys::Node`；ctor 收 `std::shared_ptr<const comfy::NodeTypeDef>` + **拷贝构造**（工厂要求）；`ClassType()` / `WidgetValue(name)`；`Draw/ToJson/FromJson` 先空实现；CMake 登记。判据：编译通过。
- **S2 插口生成** — 构造里：`isWidget` 的输入**不建 socket**（画控件），其余建输入 socket（`AllowedTypes = {type}`）；每个 output 建输出 socket；`SetName(def->displayName)`。判据：`KSampler` 出现 4 个输入插口 + 1 个 `LATENT` 输出插口。
- **S3 控件绘制（`Draw`）按 v2 字段渲染** — 按 `InputDef::widget` 与字段渲染：`Int`→`InputInt`（`display=="slider"` 用 `SliderInt(min,max)`）、`Float`→`InputFloat`（`display=="knob"` 用 `SliderFloat`、否则 `DragFloat` 带 `step`）、`Bool`→`Checkbox`（标签用 `labelOn`/`labelOff`，缺省用输入名）、`Text`→`InputText`（`multiline` 用 `InputTextMultiline`，`placeholder` 走 `imgui_stdlib`）、`Combo`→`Combo(options)`；**`forceInput==true` 的不画控件、建插口**；`advanced==true` 的折进「高级」静态分组；宽度按 `ParentArea->GetZoomFactor()` 缩放（参考 `FloatVariableNode::Draw()`）；聚焦时不吃键。判据：50%/200% 缩放下同步缩放不溢出；`seed` 按 `display` 显示为 slider 或输入框、`sampler_name` 是下拉且选项非空。
- **S4 节点尺寸自适应** — 按"控件行数 + 插口行数"算高度 `SetSize()`，避免 200×80 挤成一团。判据：`KSampler` 控件全部可见不重叠。
- **S5 存盘（`ToJson` / `FromJson`）** — `ToJson()` = `Node::ToJson()` + `"Widgets": {"seed": 42, ...}`；`FromJson()` 读回，**类型不符回落默认值、不抛异常**。判据：改 `seed=777` / `steps=33` → 存盘重启后值仍在。
- **S6 插口规格稳定性** — 保证同一 `className` 每次创建的插口**数量与顺序一致**（否则旧存档 `FromJson` 重建插口会错位）；写成代码注释 + 单测点。判据：连续创建 3 个 `KSampler` 插口顺序一致。
- **S7 验收自测（4 条）** — 跑任务说明验收 1–4，结果贴 `../PROGRESS.md`。

---

**证据（2026-09-16 完成，随 P3.3–P3.6 一起真机验收）**

- 新增 `src/graph/ComfyNode.h/.cpp`（`ShineComfyNode`）；`CMakeLists.txt` 登记。
- **与本文的一处偏差（已在头文件写明理由）**：`isWidget` 的输入**也建了插口**（不是"不建"）。原因：`NodeArea::SocketToPosition()` 把插口按 `Size.y` **均分**到整块高度，若控件输入不建插口，剩下的插口会被摊到整块高度、与控件行重叠（违反"控件不重叠"验收）。现在**输入与插口 1:1** → 每个输入 = 一行，控件画在该行右半；副作用是天然支持 ComfyUI 的"把控件改成输入"（有连线用连线值、无连线用控件值 —— 编译器就是这么写的）。
- 控件按 v2 字段渲染：`Int`（`display=="slider"` → SliderInt，否则 DragInt 且步长取 `step`）、`Float`（`knob`/`slider` → SliderFloat，否则 DragFloat）、`Bool`（Checkbox + `label_on`/`label_off` 文本）、`Text`（`multiline` → `InputTextMultiline`；`placeholder` → `InputTextWithHint`）、`Combo`（下拉，选项来自 `options`）；`forceInput` 不画控件；首个 `advanced` 前插分隔线 + "高级"。
- 尺寸/缩放：`rowPitch = max(NODE_HEIGHT_PER_SOCKET*zoom, ImGui::GetFrameHeightWithSpacing())`，每帧 `SetSize()` 让 VNS 均分插口；宽度与位移都乘 `zoom`（与 VNS 标准节点同一套做法），50%/200% 下同步缩放。
- 存盘：`ToJson()` = `Node::ToJson()` + `"Widgets"`（统一字符串值）；`FromJson()` 宽容读回（类型不符保留默认值），`SetWidgetValue()` 归一化（int 取整并夹 `min/max`、float 夹范围、bool 转 `true/false`、combo 不在选项里回落第一项）。
- 插口规格稳定：由 `NodeTypeDef.inputs` 顺序决定，且 `SetCanBeDeletedByUser(false)` —— 同一 className 每次创建数量/顺序一致。
- 实测（无头验收程序，临时已删）：7 节点建图成功、`KSampler` 输入插口数 == 定义输入数、输出 1 个 LATENT、控件值读写正确、真机 2173 类注册后建图正常。

---

## P3.3 — 动态注册 + 节点面板动态化  ✅ 7/7


- **S1 动态注册接口** — 改 `src/graph/GraphHost.h/.cpp`：`CatalogEntry` / `CatalogGroup` / `RegisterComfyNodes(defs)` / `NodeCatalog()` / `IsRegistered(className)`；`std::unordered_set<std::string> g_registered` 保证**幂等**（重复调用跳过）。判据：连续调用两次 `RegisterComfyNodes` 不重复注册、不报错。
- **S2 类型注册 + 插口分色** — 每个类型 `NODE_FACTORY.RegisterNodeType(className, ctor, copyctor)`；`NODE_SYSTEM.AssociateSocketTypeToColor` 给 `MODEL/CLIP/VAE/LATENT/IMAGE/MASK/CONDITIONING/CONTROL_NET` 分色。判据：新建节点连线颜色区分明显。
- **S3 节点面板动态化** — 改 `src/app/App.cpp:180-231` `DrawNodesPanel`：删除硬编码 `cats[]`（`App.cpp:202-212`），改读 `graph::NodeCatalog()`；保留分类折叠 + 现有搜索框 `##filter` + `ImGuiListClipper`；**`deprecated` / `experimental` 的节点标灰并在 tooltip 提示**（字段由 P3.1 的 `NodeTypeDef` 提供）。判据：面板出现按 category 分组的真实节点，搜索 `ksampler` 命中，废弃节点可见但标灰。
- **S4 创建方式两条都通** — 单击 `SpawnNode(type,x,y)` 与拖拽载荷 `"SHINE_NODE_TYPE"`（`App.cpp:224,355`）都能建节点，插口与定义一致。
- **S5 离线兜底** — `g_registered` 为空时显示 6 个 Shine 占位类型并提示"未连接 ComfyUI，显示内置示例节点"；把 `kNodeDefs`（`GraphHost.cpp:60-67`）降级为兜底数据。判据：未连接时不报错、能建占位节点。
- **S6 旧存档容错** — 加载含旧类型（`ShineSampler` 等）的 `graph.json` 不崩溃，未注册类型跳过并 `log::Warn`。判据：手工造一个含未知类型的存档复现。
- **S7 验收自测（4 条）** — 跑任务说明验收 1–4，结果贴 `../PROGRESS.md`。

---

**证据（2026-09-16 完成，随 P3.4–P3.6 一起真机验收）**

- `src/graph/GraphHost.h/.cpp` 新增 `CatalogEntry` / `CatalogGroup` / `RegisterComfyNodes()` / `NodeCatalog()` / `IsRegistered()` / `RegisteredComfyNodeCount()`；`kNodeDefs`（6 个 Shine 占位）**降级为离线兜底**（目录为空时才列出，并提示"未连接 ComfyUI，显示内置示例节点"）。
- 幂等：`std::unordered_set g_registered`；实测第二次调用 `新增 0，跳过 6（已注册/失败）`。
- 插口分色：`RegisterSocketColors()` 给 `MODEL/CLIP/VAE/LATENT/IMAGE/MASK/CONDITIONING/CONTROL_NET/ANY` 分色。
- **自动注册时机**：`DrawCanvas()` → `SyncComfyCatalog()`，每帧比对 `ComfySession::ObjectInfoNodes().size()`，变化才注册 —— 用户点「刷新 object_info」后自动生效（不需要额外按钮/接线）。
- 面板：`App.cpp::DrawNodesPanel` 删除硬编码 `cats[]`，改读 `NodeCatalog()`：分类折叠（默认折叠，按需展开）+ `ImGuiListClipper` + 搜索框（匹配显示名/类名/分类，大小写不敏感）；`deprecated`/`experimental` **标灰 + tooltip**；单击 `SpawnNode()`、拖拽载荷 `"SHINE_NODE_TYPE"` 两条创建方式都保留。
- 旧存档容错：`LoadGraph()` / `ImportGraphFile()` 前用 `WarnUnknownTypes()`（递归扫 `NodeType` 字段）对未注册类型打中文告警（VNS 会跳过这些节点）。
- 真机实测（ComfyUI v0.36.0）：`object_info cached: 2173 个节点类可查询（format v1，2159 个含输入，共 14037 个输入）` → `注册 ComfyUI 节点：新增 2167，跳过 6（已注册/失败），目录共 2173 个`；目录样例 `3d(16) 3d/mesh(10) 3d/splat(7)`。

---

## P3.4 — 图编译器：VNS 图 → ComfyUI API JSON  ✅ 7/7


- **S1 新建 `GraphCompiler.h/.cpp` 骨架** — `CompileError` / `CompileResult` / `CompileToApiJson()`；空实现返回 `ok=false`；CMake 登记。判据：编译通过。
- **S2 `EnumerateLinks()`** — 改 `src/graph/GraphHost.cpp`：从节点侧反查连线（`GetNodesConnectedToInput/Output` + `GetSocketIDByIndex`），**不改 `third/VisualNodeSystem`**。判据：能列出每条连线的（上游节点、输出槽位、下游节点、输入槽位）。
- **S3 节点 id 发号（确定性）** — 按画布遍历顺序从 `1` 递增映射成字符串 id。判据：同一张图连续两次编译**字节完全相同**。
- **S4 inputs 映射** — 插口 `Name` → ComfyUI `inputs` 键；控件值按 `Widgets` 键名写入；连线写 `["<上游id>", <输出槽位>]`。判据：`CheckpointLoaderSimple → CLIPTextEncode ×2 → KSampler → VAEDecode → SaveImage` 编译结果与手写样例逐字段一致。
- **S5 Reroute 透传 + 忽略项** — 遇 Reroute/旁路继续向上游找最近非透传节点；`GroupComment` / 注释框不进 JSON。判据：含 2 条 Reroute 的图编译后连线指向真实节点（不是 Reroute 自身）。
- **S6 必填校验** — `required` 输入既无连线也无控件值 → 收集 `CompileError`（中文、含节点显示名与缺失输入名），**不生成 JSON**（`ok=false`）。判据：故意不连 `model` → 错误指到具体节点与输入名；空图 → 提示"画布为空"。
- **S7 验收自测（4 条）** — 跑任务说明验收 1–4；其中第 2 条要把编译结果直接 POST 到 `/prompt` 确认能出图，结果贴 `../PROGRESS.md`。

---

**证据（2026-09-16 完成，含真机出图）**

- 新增 `src/graph/GraphCompiler.h/.cpp`（`CompileError`/`CompileResult`/`CompileToApiJson()`）；`GraphHost` 新增 `CanvasNodes()`（画布遍历顺序 → id 发号顺序，确定性）与 `EnumerateLinks()`（**从节点侧反查**：`GetSocketByIndex` + `NodeSocket::GetConnectedSockets()` + `GetSocketIndexByID` 的等价实现，**不改 `third/VisualNodeSystem`**；透传节点最多 16 跳防环）。
- 编译规则：连线 `["<上游 id>", <输出槽位>]`；控件按 `InputDef::widget` 写成 **JSON 数字/布尔/字符串**（不是字符串化数字）；可选输入缺省可省略；必填既无连线也无值 → **不产出 JSON** + 中文错误（含节点显示名 + 输入名）。
- 忽略项：`GroupComment` 不在 `NodeArea::Nodes` 里 → 天然不进 JSON；VNS 的连线拐点（`Connection::RerouteNodes`）对节点侧不可见 → 天然按直连处理。
- 实测（无头验收程序，临时已删）：

---

## P3.5 — 提交接线（运行 / 中断 / 错误回显）  ✅ 6/6


- **S1 `GraphHost` 提交接口** — 新增 `RunCurrentGraph()` / `LastPromptId()` / `LastCompileErrors()`；`RunCurrentGraph` 内部：`CompileToApiJson()` → 校验 → `ComfySession::SubmitPromptJson(json, cb)`。判据：编译失败时不发网络请求。
- **S2 工具条按钮** — 改 `src/app/App.cpp:322-364` `DrawGraphPanel`：加「运行」「中断」；`App.cpp:880-887` 处理 `Ctrl+Enter`。判据：按钮可点、中断走 `ComfySession::Interrupt`。
- **S3 成功路径回显** — 记录 `promptId` 并在状态栏 + 底栏「队列」页（`App.cpp:452-544`）显示；未连接 ComfyUI 时直接提示不发请求。判据：点运行 → 队列出现任务 → 进度由 WS 驱动刷新 → 完成时日志出现 `executed` 文件名。
- **S4 失败路径回显** — 按 `Doc/RULES-COMFY.md` §12.3 第 3 条解析 `/prompt` 400 的 `error` + `node_errors`，UI 显示**节点级中文错误**（不是原始 JSON）。判据：模型名不存在时错误可读、不崩。
- **S5 复核 `Doc/RULES-COMFY.md` §12** — 执行期间状态栏显示「执行中 · 节点 X（N/M）」；此时点「测试连接」若超时只打 `service busy, probe timeout ignored` 且连接状态不变；失败时日志出现 §12.5 的多行错误块。判据：日志原文可对照。
- **S6 验收自测（6 条）** — 跑任务说明验收 1–6（含第 6 条"断线 10s 再恢复仍能补出错误、不出现永远运行中"），结果贴 `../PROGRESS.md`。**P3 主线到此结束，可转 P4。**

---

**证据（2026-09-16 完成，真机提交）**

- `GraphHost` 新增 `RunCurrentGraph()` / `InterruptCurrentGraph()` / `LastPromptId()` / `LastCompileErrors()`；**未连接 ComfyUI 时不发任何请求**，只记录一条中文提示。
- UI 接线：`DrawGraphPanel` 加「▶ 运行」「■ 中断」+ 编译/提交错误列表（节点显示名 + 中文，红色）；状态栏加 `prompt <前 8 位>` 与「错误 N」；底栏「队列」顶部显示最近提交的 prompt；`Ctrl+Enter` 运行（带 `!io.WantTextInput` 守卫）。
- 实测（无头验收程序 + 真机 ComfyUI v0.36.0）：
- §12 复核：执行期间状态栏走 `HealthSummary()`/`BusyDetail()`（P3.0 已实现）；忙碌时探活超时只打 `service busy, probe timeout ignored`、不改连接状态（P3.0 S8 已验）。
- **未自动验证（需人工目视）**：「中断」按钮的实际点击路径、UI 文案观感。

---

## P3.6 — 工作流导入 / 导出  ✅ 9/9


- **S1 骨架 + 格式判定** — 新增 `src/graph/WorkflowIO.h/.cpp`（`WorkflowFormat` / `DetectFormat` / `ImportReport` / 四个读写函数 / `ClearGraph`）；CMake 登记。判据：`DetectFormat` 对三类样例（自家 graph.json / API / 工作流 v1.0）判定正确。
- **S2 导出 API 格式** — `ExportApiJson` = `CompileToApiJson()` 落盘（utf-8，失败返回 `false` + `log::Error`）。判据：导出文件能被 `yyjson` 正常解析。
- **S3 导入 API 格式：建节点** — `class_type` → 已注册类型（`IsRegistered` / `CreateNodeByType`），无坐标时按网格自动排列。判据：官方默认 txt2img API JSON 能建出 6 个节点。
- **S4 导入 API 格式：连线与控件值** — `inputs` 里值是 `["<id>", idx]` → 连线；其余写进 `Widgets`；完成后 `CenterView()`。判据：导入后节点/连线/控件值与源 JSON 语义一致。
- **S5 导出工作流 v1.0** — 按 §12.6 C 写 `version:1` + `state` + `nodes`（`id`/`type`/`pos`/`size`/`flags`/`order`/`mode`/`properties`/`widgets_values`）+ `links`（`id`/`origin_id`/`origin_slot`/`target_id`/`target_slot`/`type`）+ `groups` + `extra.ds{scale,offset}`；`pos`/`size` 写**数组形式**；节点 id 用 `GraphCompiler` 的确定性发号，link id 从 1 递增（**两次导出逐字节一致**）。判据：文件用 `yyjson` 解析通过且字段齐全。
- **S6 导入工作流 v1.0（兼容 0.4）** — 解析 `nodes`/`links`/`groups`/`reroutes`/`extra.ds`；`widgets_values` 按**输入定义顺序**（`FindNodeDef`）回填并校验顺序一致（不一致则 `warnings` + 按名尽量对齐）；`reroutes` 透传解析、转换为直连、数量记进 `ImportReport.reroutes`；`version != 1` 走 0.4 兼容路径并告警。判据：导入 ComfyUI 前端保存的工作流，节点/连线/控件值正确。
- **S7 未知类型不静默丢弃** — 收集 `unknownTypes`，在 `DrawWorkflowsPanel` 列出清单并提示"先连 ComfyUI 刷新 object_info"。判据：含未知节点的 JSON 其余节点正常建出 + 面板有清单。
- **S8 接 UI 入口** — 改 `src/app/App.cpp:233-258` `DrawWorkflowsPanel`：接通「导入工作流 / API JSON...」（当前是 `log::Warn` 占位，`App.cpp:253-255`）+「导出」两项（导出格式按用户选择：API / 工作流 v1.0）；`SaveGraph/LoadGraph` 增加格式分支（按 `DetectFormat`，不看扩展名）。判据：菜单能选文件、导入后画布立即刷新。
- **S9 验收自测（5 条）** — 跑任务说明验收 1–5，其中 **第 3、4 条需要本机 ComfyUI 前端配合**（导出 → 前端打开；前端保存 → 导入）；结果贴 `../PROGRESS.md`。

---

**证据（2026-09-16 完成）**

- 新增 `src/graph/WorkflowIO.h/.cpp`（`WorkflowFormat`/`DetectFormat`/`ImportReport`/`ExportApiJson`/`ImportApiJson`/`ExportWorkflowV1`/`ImportWorkflowJson`）；新增 `src/app/FileDialog.h/.cpp`（`IFileOpenDialog`/`IFileSaveDialog`，返回 UTF-8 路径）；`GraphHost::ImportGraphFile()` 按**内容**分支（API / 工作流 v1.0 / 0.4 / 自家 VNS 图）。
- UI：`DrawWorkflowsPanel` 接通「导入文件...」「导出 API JSON...」「导出工作流 v1.0...」三个入口（原先只有 `log::Warn` 占位）。
- 实测（无头验收程序，临时已删）：
- **未在本机完成的一条**：验收第 3 条"导出的工作流用 ComfyUI **前端**打开"需要人工在浏览器点一次（AI 无法操作前端 UI）；文件结构已按 `Doc/RULES-COMFY.md` §12.6 C 生成，且能被 `yyjson` 正常解析、能被本项目自己完整导入还原。

---

## P3.7 — 从 ComfyUI 拉取工作流（远端导入）  ✅ 3/3

> **由来（2026-09-17 用户）**：「你应该获取 ComfyUI 的工作流，我记得可以获取的，就是导出 API，然后去解析它的 API，生成节点」。
> 目标：**不要手写图**（P5.4 照 Python 源码手写过，栽了 autogrow 子键 1 基的坑），而是把服务器自己的工作流取下来 → 解析 → 生成节点，再在此基础上改。

- **S1 拉取模块** — 新增 `src/comfy/ComfyWorkflows.h/.cpp`：`FetchWorkflowListAsync(baseUrl, cb)`（worker 拉三来源清单，回调在 UI 线程）
  + `FetchWorkflowAsync(baseUrl, ref, cb)`；三个纯解析函数 `ParseTemplateIndex/ParseCustomNodeTemplateList/ParseUserWorkflowList` +
  `BuildUserWorkflowFetchPath`（转义口径对外可见）。CMake 登记。
  **三来源端点（2026-09-17 在 ComfyUI 0.36.0 上逐个实测 200，不是照文档猜的）**：
  ① 内置模板 `GET /templates/index.json`（571 KB / 502 条）→ 正文 `GET /templates/<name>.json`；
  ② 节点包示例 `GET /workflow_templates`（`{模块: [名]}` / 144 条）→ `GET /api/workflow_templates/<模块>/<名>.json`；
  ③ 我保存的 `GET /userdata?dir=workflows&recurse=true` → 正文 `GET /userdata/workflows%2F<相对路径>`（**斜杠必须 `%2F`**；目录不存在时 404 视为"空"）。
  判据：离线自测 **PASS 37 / FAIL 0**（用磁盘上真实的 571 KB `index.json` 与真实 `/workflow_templates` 响应片段作样本；含中文/空格路径、损坏与结构不符输入）。
- **S2 独立模板窗 + 接线**（用户 2026-09-17 追加要求：「模板不要放在侧栏，工具栏给按钮弹出新窗口，左边分类、右边工作流，点击才创建」）——
  新增 `app/dialogs/DrawTemplateWindow.h/.cpp`：`UiState.showTemplateWindow` + 图工具条「模板」按钮 + 菜单「文件 → 从模板新建工作流...」+
  侧栏「工作流」里的入口按钮；窗口内 **左分类（含计数）/ 右条目表**（`ImGuiTable` + clipper + 搜索框 + 悬停详情），
  **单击条目才创建**（画布非空先弹「替换当前画布？」确认），成功即关窗回画布；中文错误 / **未注册类名清单**（不静默丢弃）照旧。
  配套 `GraphHost::ImportGraphText(text, label, ImportReport*)`（远端文本没有本地文件）+ `ImportGraphFile` 退化成"读盘 + 调它"。
  真机自检钩子 `SHINE_WORKFLOW_SELFTEST=<模板名>`（报告 `%TEMP%\shine_p37_report.txt`）随窗口迁移，并**自带开窗 + 选中目标所在分类**（截图状态确定）。
  判据：截图可见左分类 17 组（「内置模板 · Video (157)」高亮）、右条目含 H3 三条，工具条多出「模板」按钮。
- **S3 真机验收 + 修连线** — 用官方 `video_minimax_h3_r2v.json`（43473 字节，编辑器格式 **v0.4**）跑通
  「拉清单 → 找目标 → 拉正文 → 识别格式 → 生成节点」，最终 **26 节点 / 33 连线**（= 官方模板全部连线；3 个 `MarkdownNote` 是前端专有节点，未注册且已如实列出）。
  过程中修掉三个真缺陷：
  ① **v0.4 的 `links` 是数组** `[id, origin_id, origin_slot, target_id, target_slot, type]`，原实现只认 v1.0 对象形式 → 非对象 `continue` → **静默丢光全部连线**（0 条）；改为两种形状都认，认不出的形状给告警。
  ② **`/object_info` 的插口顺序 ≠ 前端 JSON `inputs[]/outputs[]` 顺序** → 只按下标连线会大量接不上（0→15→24 条）；加"按 JSON 里的插口**名字**重找槽位"兜底（`父.子` 归到父插口）。
  ③ **ComfyUI 插口类型语义比 VNS 宽**：并集（`"FLOAT,INT,BOOLEAN"`）、通配（`*`/`ANY`/`COMFY_MATCHTYPE_*`）、动态父插口（`COMFY_AUTOGROW_V3`/`COMFY_DYNAMICCOMBO_V3`）；
  VNS 只做"两边 `AllowedTypes` 求交集"（`VisualNode.cpp:176-211`）→ 这几类永远接不上。已让 `ShineComfyNode::BuildSockets()` 拆并集 + 给通配补 `ANY`，
  并 **override `IsValidAsNewConnection`**（必须连这个一起覆盖：`NodeSystem.cpp:1967` 会用它**复检并断开**已有连线）。
  ④ 顺带修 UI 缺陷：官方模板坐标在 `y≈4850`，而 VNS 的 `CenterViewOnAllElements()` 在目标偏移超出 `GRID_SIZE` 时**静默返回 false**
  （`VisualNodeAreaRendering.cpp:763-767`）→ "导入成功、计数正确、画布空白"。现在导入后把整图平移到原点附近（相对布局不变）再 `CenterView()`。
  **验收**：`SHINE_SIDE_VIEW=workflows` + 钩子 + `scripts/capture_window.ps1`；报告 PASS + 截图显示 `26 节点 · 33 连线 · 缩放 20%`
  且节点/连线/分组注释框可见；ComfyUI 起停干净（用完 `Stop-Process`）。

---
